
import { IAppUser } from '../../../types/user.type';
import { baseApi } from '../../api/baseApi';
import { IParam, IResponse } from './util.type';

const userApi = baseApi.injectEndpoints({
  endpoints: builder => ({
  
  }),
});

export const {
 
} = userApi;
